# $FreeBSD$

PACKAGE=	tests
TESTSPACKAGE=	${PACKAGE}
TESTSDIR=	${TESTSBASE}/bin/sh/${.CURDIR:T}

.PATH: ${.CURDIR:H}
ATF_TESTS_SH=	functional_test

TESTS=		and1.0
TESTS+=		and2.1
TESTS+=		and3.0
TESTS+=		and4.0
TESTS+=		background1.0
TESTS+=		cmd1.0
TESTS+=		cmd2.1
TESTS+=		elif1.0
TESTS+=		elif2.0
TESTS+=		eval1.0
TESTS+=		eval2.1
TESTS+=		for1.0
TESTS+=		func1.0
TESTS+=		func2.1
TESTS+=		if1.0
TESTS+=		if2.0
TESTS+=		if3.0
TESTS+=		not1.0
TESTS+=		not2.0
TESTS+=		or1.0
TESTS+=		or2.0
TESTS+=		or3.1
TESTS+=		pipe1.1
TESTS+=		pipe2.0
TESTS+=		return1.0
TESTS+=		semi1.1
TESTS+=		semi2.1
TESTS+=		subshell1.0
TESTS+=		subshell2.1
TESTS+=		until1.0
TESTS+=		until2.0
TESTS+=		until3.0
TESTS+=		while1.0
TESTS+=		while2.0
TESTS+=		while3.0

.include <bsd.test.mk>
