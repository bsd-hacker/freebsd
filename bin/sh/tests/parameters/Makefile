# $FreeBSD$

PACKAGE=	tests
TESTSPACKAGE=	${PACKAGE}
TESTSDIR=	${TESTSBASE}/bin/sh/${.CURDIR:T}

.PATH: ${.CURDIR:H}
ATF_TESTS_SH=	functional_test

TESTS=		env1.0
TESTS+=		exitstatus1.0
TESTS+=		mail1.0
TESTS+=		mail2.0
TESTS+=		optind1.0
TESTS+=		optind2.0
TESTS+=		positional1.0
TESTS+=		positional2.0
TESTS+=		positional3.0
TESTS+=		positional4.0
TESTS+=		positional5.0
TESTS+=		positional6.0
TESTS+=		positional7.0
TESTS+=		positional8.0
TESTS+=		positional9.0
TESTS+=		pwd1.0
TESTS+=		pwd2.0

.include <bsd.test.mk>
