# $FreeBSD$

PACKAGE=	tests
TESTSPACKAGE=	${PACKAGE}
TESTSDIR=	${TESTSBASE}/bin/sh/${.CURDIR:T}

.PATH: ${.CURDIR:H}
ATF_TESTS_SH=	functional_test

TESTS=		bg1.0
TESTS+=		bg2.0
TESTS+=		bg3.0
TESTS+=		bg4.0
TESTS+=		bg5.0
TESTS+=		bg6.0 bg6.0.stdout
TESTS+=		bg7.0
TESTS+=		bg8.0
TESTS+=		bg9.0
TESTS+=		bg10.0 bg10.0.stdout
TESTS+=		fork1.0
TESTS+=		fork2.0
TESTS+=		fork3.0
TESTS+=		func1.0
TESTS+=		func2.0
TESTS+=		func3.0
TESTS+=		hash1.0
TESTS+=		int-cmd1.0
TESTS+=		killed1.0
TESTS+=		killed2.0
TESTS+=		not1.0
TESTS+=		not2.0
TESTS+=		path1.0
TESTS+=		redir1.0
TESTS+=		redir2.0
TESTS+=		redir3.0
TESTS+=		redir4.0
TESTS+=		redir5.0
TESTS+=		redir6.0
TESTS+=		redir7.0
TESTS+=		set-n1.0
TESTS+=		set-n2.0
TESTS+=		set-n3.0
TESTS+=		set-n4.0
TESTS+=		set-x1.0
TESTS+=		set-x2.0
TESTS+=		set-x3.0
TESTS+=		set-x4.0
TESTS+=		shellproc1.0
TESTS+=		subshell1.0 subshell1.0.stdout
TESTS+=		subshell2.0
TESTS+=		subshell3.0
TESTS+=		subshell4.0
TESTS+=		unknown1.0
TESTS+=		var-assign1.0

.include <bsd.test.mk>
