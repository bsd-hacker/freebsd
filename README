This contains stuff which shall allow to run a desktop (or more correctly,
a X server) in a jail. This gives more access to the raw machine than
what is allowed normally in a jail. Everything is supposed to be properly
protected (disabled by default).
